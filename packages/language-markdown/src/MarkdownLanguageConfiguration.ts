import {
  RichLanguageConfiguration,
  RichLanguageFormat
} from "@dc-extension-rich-text/common";

const conf: RichLanguageConfiguration = {
  format: RichLanguageFormat.TEXT
};

export default conf;
