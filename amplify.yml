version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm install 22
        - nvm use 22
        - yarn install --frozen-lockfile --ignore-scripts
        # - yarn test
    build:
      commands:
        - CI=false yarn build
  artifacts:
    baseDirectory: packages/extension/build
    files:
      - '**/*'
